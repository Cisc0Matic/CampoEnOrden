[build]
  base = "frontend/campoenorden_frontend"
  command = "npm install --include=dev && npm run build"
  publish = "www"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
